---
created_at: <CREATED_AT>
---

# Design: <task-name>

## 決策清單

<以下是 Claude 提出的設計決策。請逐項審查,確認後勾選 checkbox。
**全部勾選後**且「待討論問題」清空後,執行 /spec:run 才會自動產 task.md 並開始執行。
若不同意某項,直接修改該項內容後勾選。
若有疑問,寫到本檔案最下方的「待討論問題」區塊。>

- [ ] **<決策 1 標題>**:<一句話描述決策>
  - 理由:<為什麼這樣選>
  - 替代方案:<考慮過但否決的選項,以及否決原因>
- [ ] **<決策 2 標題>**:...
  - 理由:
  - 替代方案:
- [ ] **<決策 3 標題>**:...
  - 理由:
  - 替代方案:

## 影響範圍

<列出此次改動會觸及的檔案、模組、合約。讓 reviewer 一眼看出 blast radius。>

- 直接改動:
- 間接影響(被呼叫端、被繼承類):
- 不影響但需注意:

## 實作細節

<可以是散文,也可以是條列。**不要寫程式碼**,程式碼是 task.md 的職責。
描述「要怎麼做」即可,例如:>

- 新增 `app/Core/Services/{Module}/Contracts/{Module}Contract.php`,定義介面方法簽章
- `{Module}Service` 改為 implements 此 contract
- 在 service provider 中綁定 contract 到 implementation
- 修改 `{Module}Controller` 注入 contract 而非 concrete class

## 降級策略(僅跨外部系統呼叫時必填)

<若此次變更涉及外部系統呼叫(CRM/Campaign/Odoo 等),依 project.md §10 必須說明:
- 外部系統失敗時如何處理(回傳預設值?拋例外?降級為快取?)
- log 等級與 context 內容
- 是否需要 retry,retry 次數與間隔>

## 最小化形式說明

<若這是小型改動,本份 design.md 可採最小化形式:
- 決策清單可只有 2~3 項
- 影響範圍可寫「單一檔案」
- 實作細節可寫「無額外細節,見 task.md」

但「決策清單」區塊**不可省略**,即使只有一條決策也要寫出來。
請在審查時刪除本段說明。>

---

## 已討論問題

> 此區塊記錄歷次討論的摘要,**由 /spec:run 在討論模式自動寫入**,使用者無需手動編輯。
>
> 用途:保留決策的演進脈絡,讓未來的 reviewer(包括幾個月後的你自己)能看到
> 「這條決策為什麼長這樣」、「曾經考慮過哪些方向」。
>
> 格式:每個已處理的問題包含「問題、結論、影響、討論時間」四個欄位。

<!-- 討論記錄會自動追加在這裡。初始狀態為空。 -->

---

## 待討論問題

> 此區塊由**使用者**填寫,用於標記需要跟 Claude 進一步討論的決策。
>
> /spec:run 執行時會檢查此區塊:
> - 若有實質內容 → 進入「討論模式」,Claude 會逐題回答並修改受影響的決策,
>   修改過的決策會被重置為 `- [ ]` 待重新審查,問題摘要會被搬到「已討論問題」,然後停下等你重新審查
> - 若空白(僅剩此說明文字)且決策全勾選 → 自動產 task.md 並逐項執行
>
> 撰寫格式建議:
> - 每題一個 bullet,清楚指明「要討論哪條決策」+「具體疑問」
> - 例如:「決策 4 - 是否要加 is_active 欄位以處理會議室停用情境?」
>
> 若無問題,請保持此區塊只有本說明文字(不需刪除整個區塊)。

<!-- 在下方寫你的問題,例如:
- 決策 X - 你的問題
-->
